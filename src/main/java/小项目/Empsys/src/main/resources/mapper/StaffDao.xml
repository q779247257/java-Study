<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.dao.StaffDao">

    <resultMap type="com.xuan.entity.Staff" id="StaffMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="age" column="age" jdbcType="INTEGER"/>
        <result property="sex" column="sex" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="StaffMap">
        select
          id, name, age, sex, phone
        from java_study.staff
        where id = #{id}
    </select>

    <select id="findRolesByName" parameterType="string" resultType="string">
SELECT
	r.role_name
FROM
	shiro_user u
	INNER JOIN shiro_user_role ur ON u.uid = ur.user_id
	INNER JOIN shiro_role r ON r.r_id = ur.role_id
WHERE
	u.user_name = #{name}
    </select>

    <select id="findPermsByName" parameterType="string" resultType="string">
SELECT
	p.power_desc
FROM
	shiro_user u
	INNER JOIN shiro_user_role ur ON u.uid = ur.user_id
	INNER JOIN shiro_role_power rp ON rp.role_id = ur.role_id
	INNER JOIN shiro_power p ON p.id = rp.power_id
WHERE
	u.user_name = #{name}
	AND p.power_desc IS NOT NULL
	AND p.power_desc != ''
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll"  resultMap="StaffMap">
        select
          id, name, age, sex, phone
        from java_study.staff
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="age != null">
                and age = #{age}
            </if>
            <if test="sex != null and sex != ''">
                and sex = #{sex}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into java_study.staff(name, age, sex, phone)
        values (#{name}, #{age}, #{sex}, #{phone})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update java_study.staff
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="sex != null and sex != ''">
                sex = #{sex},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from java_study.staff where id = #{id}
    </delete>

    <select id="count" resultType="int">
        select count(1) from java_study.staff;
    </select>

    <!--批量插入-->
    <insert id="addBatch" parameterType="list">
        insert into
        java_study.staff
        (id, name, age, sex, phone)
        values
        <foreach collection="list" item="emp" separator=",">
            (#{emp.id}, #{emp.name}, #{emp.age}, #{emp.sex}, #{emp.phone})
        </foreach>
    </insert>

</mapper>